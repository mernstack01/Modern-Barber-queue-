# Packages

Shared workspace packages can live here as the SaaS grows. The current MVP keeps app-specific code inside each feature module while preserving the monorepo structure requested for future shared UI, config, or domain packages.
